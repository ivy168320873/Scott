# Scott Studio（AI 影片／短劇生成平台）相依。
#
# 與 requirements.txt 分離的理由：既有股市分析 app 不需要這些套件，
# 分離後 `python app.py` 的部署路徑不受影響，Studio 相依安裝失敗也不會拖垮主程式。
#
# 版本釘選以確保部署可重現（沿用 Scott 既有慣例）。
# anthropic 刻意與 requirements.txt 使用相同版本，避免兩份清單同時安裝時衝突。

# ── Web / API ────────────────────────────────────────────────────────────────
fastapi==0.140.7
uvicorn[standard]==0.51.0
python-multipart==0.0.32
a2wsgi==1.10.10

# ── 設定 ──────────────────────────────────────────────────────────────────────
pydantic==2.13.4
pydantic-settings==2.14.2

# ── 資料庫 ────────────────────────────────────────────────────────────────────
SQLAlchemy[asyncio]==2.0.51
alembic==1.18.5
aiosqlite==0.22.1
asyncpg==0.31.0
greenlet==3.5.4

# ── 任務佇列 ──────────────────────────────────────────────────────────────────
celery==5.6.3
redis==8.0.1

# ── 物件儲存 ──────────────────────────────────────────────────────────────────
boto3==1.43.57

# ── 供應商 SDK ────────────────────────────────────────────────────────────────
httpx==0.28.1
anthropic==0.109.2

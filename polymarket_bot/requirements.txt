# --- Web / async runtime ---
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2

# --- HTTP / WebSocket clients ---
httpx>=0.27
websockets>=12.0

# --- Database (async PostgreSQL) ---
asyncpg>=0.29

# --- News ingestion ---
feedparser>=6.0

# --- LLM classifiers (optional at runtime; pick one) ---
anthropic>=0.40
openai>=1.30

# --- Utilities ---
python-dotenv>=1.0
tenacity>=8.2

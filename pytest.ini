[pytest]
testpaths = tests
# 嚴格模式：未註冊的 marker 與設定錯誤直接視為失敗，避免測試被靜默略過。
addopts = -q --strict-markers --strict-config
# 把警告當錯誤會讓第三方套件的 DeprecationWarning 打斷 CI，
# 因此只顯示不攔截；真正該修的問題由測試本身斷言。
filterwarnings =
    default
